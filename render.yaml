databases:
  - name: gwent-db
    plan: free

services:
  # The Backend Game Server
  - type: web
    name: gwent-server
    runtime: docker
    repo: https://github.com/DJ-EJ-L/gwent-classic-mp.git
    plan: free
    dockerContext: ./server # Reverted to simple path
    dockerfilePath: ./server/Dockerfile # Reverted to simple path
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: gwent-db
          property: connectionString
      - key: PORT
        value: 5000

  # The Frontend Web Client
  - type: web
    name: gwent-web
    runtime: docker
    repo: https://github.com/DJ-EJ-L/gwent-classic-mp.git
    plan: free
    dockerContext: ./web # Reverted to simple path
    dockerfilePath: ./web/Dockerfile # Reverted to simple path
    envVars:
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: gwent-server
          property: host
      - key: PORT
        value: 3000
